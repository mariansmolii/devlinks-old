export const selectLinks = (state) => state.links.links;

export const selectIsLoading = (state) => state.links.isLoading;

export const selectLinksError = (state) => state.links.error;

export const selectIsLinksInDB = (state) => state.links.isLinksInDB;
